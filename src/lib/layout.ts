/**
 * The single margin unit used for spacing every desktop-edge element —
 * desktop icons, the featured rail, and the Terminal's default position —
 * so nothing on the first-boot screen ever overlaps. Mirrors the
 * `--desktop-margin` custom property in styles/desktop.css; keep both in
 * sync if this changes.
 */
export const DESKTOP_MARGIN_PX = 26;
